# Backend TrincaShop - Variáveis de Produção

SPRING_PROFILES_ACTIVE=prod

# --- NEON SERVERLESS POSTGRES ---
# 1. Crie seu projeto no Neon (neon.tech)
# 2. Copie a Connection String. Ela se parece com: postgresql://[user]:[password]@[host]/neondb?sslmode=require
# 3. Formate a DATABASE_URL trocando 'postgresql://' por 'jdbc:postgresql://'

DATABASE_URL=jdbc:postgresql://[seu-host-neon]/[nome-do-banco]?sslmode=require
DATABASE_USERNAME=seu_usuario
DATABASE_PASSWORD=sua_senha_segura

# --- SEGURANÇA JWT ---
JWT_SECRET=super_secret_key_jwt_producao_256bits_minimo
