server:
  port: 8080

spring:
  application:
    name: trincashop-backend

# ============================================
# CONFIGURAÇÃO NEON POSTGRESQL (DESATIVADA)
# Descomentar quando quiser integrar com o banco real
# ============================================
#  datasource:
#    url: jdbc:postgresql://<NEON_HOST>/<NEON_DATABASE>?sslmode=require
#    username: <NEON_USER>
#    password: <NEON_PASSWORD>
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
